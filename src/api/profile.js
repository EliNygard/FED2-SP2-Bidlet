// From Chat getParseTreeNode, test when api is in place

// import '../js/components/UserProfile.js';

// async function loadUserProfile() {
//   try {
//     const userName = api.user.name;
//     const profile = await api.profiles.readSingleProfile(userName);

//     const userProfile = document.createElement('user-profile');
//     userProfile.name = profile.name;
//     userProfile.bio = profile.bio;

//     document.getElementById('profileCard').appendChild(userProfile);
//   } catch (error) {
//     console.error('Error fetching profile:', error);
//     alert('Failed to load profile.');
//   }
// }

// loadUserProfile();
